
export const metadata = {
    title: 'about',
    description: 'Generated page',
}

export default function RootLayout({ children }) {
    return (
        <div>
            {children}
        </div>

    )
}
